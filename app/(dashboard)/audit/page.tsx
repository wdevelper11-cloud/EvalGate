import { PageHeader } from "@/components/page-header";

const events = [["Test case created", "Scenario registry event"], ["Prompt version created", "Configuration registry event"], ["Evaluation run completed", "Runner lifecycle event"], ["Release decision generated", "Readiness gate event"]] as const;
export default function AuditPage() { return <><PageHeader eyebrow="Traceability" title="Audit timeline" description="Follow the lifecycle of evaluation assets and release decisions in chronological order." /><div className="mt-7 rounded-lg border border-teal-400/20 bg-teal-400/5 px-4 py-3 text-xs text-teal-200">Preview events below demonstrate the timeline layout only; they are not real activity.</div><ol className="panel mt-6 divide-y divide-slate-800 px-6">{events.map(([title, detail], index) => <li className="relative py-6 pl-9" key={title}><span className="absolute left-0 top-7 grid h-5 w-5 place-items-center rounded-full border border-teal-400/30 bg-teal-400/10 text-[9px] text-teal-300">{index + 1}</span><div className="flex flex-col justify-between gap-2 sm:flex-row"><div><h2 className="text-sm font-semibold text-white">{title}</h2><p className="mt-1 text-sm text-slate-500">{detail}</p></div><span className="text-xs text-slate-600">Placeholder event</span></div></li>)}</ol></>; }
