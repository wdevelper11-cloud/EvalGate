import { PageHeader } from "@/components/page-header";
import { ReleaseDecisionBadge } from "@/components/release-decision-badge";

const summary = [["Selected prompt", "Not selected"], ["Number of tests", "—"], ["Pass / fail", "— / —"], ["Average score", "—"], ["Safety failures", "—"]] as const;
export default function ReportsPage() { return <><PageHeader eyebrow="Evidence" title="Evaluation reports" description="Review aggregate test outcomes and the final release recommendation for a completed run." /><div className="mt-7 rounded-lg border border-teal-400/20 bg-teal-400/5 px-4 py-3 text-xs text-teal-200">Skeleton report only · no evaluation data has been generated or persisted.</div><section className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{summary.map(([label, value]) => <article className="panel p-5" key={label}><p className="text-xs uppercase tracking-wider text-slate-500">{label}</p><p className="mt-3 text-xl font-semibold text-white">{value}</p></article>)}<article className="panel p-5"><p className="text-xs uppercase tracking-wider text-slate-500">Final release decision</p><div className="mt-4"><ReleaseDecisionBadge /></div></article></section><section className="panel mt-6 overflow-hidden"><div className="border-b border-slate-800 p-5"><h2 className="font-semibold text-white">Recent results</h2><p className="mt-1 text-sm text-slate-500">Individual test evidence from the selected run.</p></div><div className="grid min-h-44 place-items-center p-6 text-sm text-slate-500">No results available.</div></section></>; }
